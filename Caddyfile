# Add this block to your HOST infra Caddy config:
#   ~/IOM/-Aviation_OCS_backend_node/infra/docker/Caddyfile
#
# RPM app runs via docker-compose on host port 9098.
# Use 172.17.0.1 (Docker bridge gateway) — same pattern as your other UAT sites.

cad.sisgain.ae {
	reverse_proxy 172.17.0.1:9098
}
